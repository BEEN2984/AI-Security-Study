# hello_llm

OpenAI API를 이용해 사용자 입력을 LLM에 전달하고 응답을 출력

## 목적

- OpenAI API 기본 호출 방식 익히기
- `.env`를 이용한 API 키 관리
- API 응답이 단순 문자열이 아닌 `Response` 객체로 반환되는 것을 확인

## 실행

```bash
pip install openai python-dotenv
python hello_llm.py
```
